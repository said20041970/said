# said

## ScrapeGraph MCP server

This repo declares the ScrapeGraph MCP server in `.mcp.json` (project scope), so anyone working in Claude Code here gets it automatically.

Before use, set your API key in the environment (never commit it):

```
export SCRAPEGRAPH_API_KEY="your-scrapegraph-api-key"
```
